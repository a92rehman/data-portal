You are given an existing React + TypeScript component named ModifyDashboardRequester that already contains state, handlers, and button actions.
Do not change any logic. Do not rename variables, props, functions, or event handlers.
Your job is visual design only using Tailwind CSS and shadcn/ui classes, applied to the existing JSX.

Absolute rules

Keep the component name, imports, hooks, event handlers, and JSX structure that wires logic together.

Do not add or remove any handler props (e.g., onClick, onChange, onValueChange).

Do not rename any state variables or functions.

You may only add Tailwind classes, basic wrapper elements (divs, Cards), and labels to achieve the layout below.

Use shadcn/ui components already in use (Button, Input, Textarea, Select, Card).

No new business logic. No new states. No conditional rendering beyond what already exists.

Page container

Wrap the page in: div.max-w-6xl.mx-auto.p-6.space-y-6.

Header card

Use a Card with p-4 shadow-md border border-gray-200.

Left:

Title h2.text-xl.font-semibold: “Modify Dashboard - Requester”.

Subline p.text-gray-600.text-sm: “Request Type: — | ID: —”.

Right: two buttons in a row:

Button variant outline → label Accept.

Button variant destructive → label Reject.

Container: div.flex.items-center.justify-between.

Info grid (top form)

Grid container: grid grid-cols-4 gap-4.

Row 1:

Input placeholder Priority.

Input type date placeholder Due Date.

Input placeholder Department/Team.

Input placeholder Requested By.

Row 2:

Input placeholder New Priority.

Input type date placeholder New Due Date.

Assigned To must be visually longer: place the existing Input inside div.col-span-2, and Input uses className=\"w-full\".

Main content grid (two columns)

Grid container: grid grid-cols-2 gap-4.

Left card — Request Details

Card.p-4 with title h3.font-semibold.mb-2: Request Details.

Textarea with rows={6}, placeholder: Enter request details here....

Right card — Delivery

Card.p-4.space-y-4 with title h3.font-semibold: Delivery.

Type (Select):

Label: block text-sm font-medium mb-1 → Type.

Select trigger placeholder: Select delivery type.

Keep existing options/values: attachment, link, other. Do not change values (they are used by logic).

Attachment row (rendered only when the existing logic shows it):

Label: Attachment; use Input type=\"file\".

Link row (rendered only when the existing logic shows it):

Label: Link URL; use Input type=\"url\" placeholder=\"https://…\".

Delivered On:

Label: Delivered On; Input type=\"date\".

Static helper text below (styling only): p.text-xs.text-gray-500.mt-1 → “Used to auto-calculate On Time vs Late using the (new) due date.”

On Time (read-only display):

Label: On Time.

Status box div with classes:

Base: rounded-md border px-3 py-2 text-sm bg-gray-50 text-gray-600.

When status is “On time”: add border-green-300 text-green-700 bg-green-50.

When status is “Late”: add border-red-300 text-red-700 bg-red-50.

Do not change how status is computed; just apply these classes based on the existing status variable.

Request Completed:

Bottom button area: div.pt-2 and Button labeled Mark as Completed. Keep the existing onClick and disabled state.

Messages / Comments card

Card.p-4.

Title h3.font-semibold.mb-2: Messages / Comments.

Textarea with rows={4} and placeholder Add your message or comment here....

Right-aligned Send button in div.flex.justify-end.mt-2.

Responsiveness & polish

On small screens, the two grids collapse to one column (Tailwind grid behavior is fine with current classes).

Keep inputs full-width inside their grid cell.

Use rounded corners and subtle shadows on Cards and Buttons. No custom CSS files; only Tailwind utilities.

Acceptance checklist (must pass)

No function names, handlers, or state variables changed.

The Assigned To field spans two columns on md+ screens.

Delivery Type shows the three options (Attachment, Link, Other) with original values.

Conditional sections (Attachment / Link URL) are still driven by the existing logic (you did not add logic).

On-time status styling changes based on the existing computed value only.

All visual spacing and typography match the classes specified above.